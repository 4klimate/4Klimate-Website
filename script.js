function randomAdvice(){
  advice = ['Ride a bike to school/work', 'turn off the faucet when not using it', 'Recycle recycleables'];

  randomValue = advice[Math.floor(Math.random() * advice.length)];
  
  console.log(randomValue);
  alert(randomValue);
}






